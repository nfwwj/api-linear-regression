To run

1. Clone repo
2. Install dependencies
3. Launch the development server by running api.py
- To run API locally, forward port 5000 (default config for Flask), and set to public. 

[API Documentation]
GET /predict/<int: x>
-> Returns expected salary where x is years of experience

4. To host on Render, deploy repo on Render
